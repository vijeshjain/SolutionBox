#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gostack/bin/gostack" "$@"
  ret=$?
else 
  node  "$basedir/../gostack/bin/gostack" "$@"
  ret=$?
fi
exit $ret
